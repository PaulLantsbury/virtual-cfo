import {useQuery} from '@tanstack/react-query';
const labels:Record<string,string>={revenue:'Booked revenue',processingFee:'Processing fees',advertising:'Advertising',software:'Software',includedCash:'Included cash'};
type Account={id:string;name:string;type:string;status:string};type Suggestion={candidates:{accountName:string;confidence:string}[]};type Preview={mappingStatus:string;categories:{category:string;confirmed:Account[];suggestion:Suggestion}[]};
async function fetchPreview(signal:AbortSignal):Promise<Preview>{const response=await fetch('http://127.0.0.1:4002/api/xero/mapping-preview',{signal,cache:'no-store'});if(!response.ok)throw Error('unavailable');return response.json() as Promise<Preview>;}
export function XeroMappingSetup(){const query=useQuery({queryKey:['xero-local-mapping-preview'],queryFn:({signal})=>fetchPreview(signal),retry:false,refetchOnWindowFocus:false});const preview=query.data;return <section aria-label="Xero account mapping" className="rounded-2xl border bg-card p-6 sm:p-8 space-y-5"><div><h2 className="text-xl font-semibold">Xero account mapping</h2><p className="mt-2 text-sm text-muted-foreground">This local test preview shows your previously confirmed Xero accounts and review-only suggestions. It does not contact Xero, save changes, or compare Xero with Shopify.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-sm"><thead className="text-left text-muted-foreground"><tr><th className="pb-3 font-medium">Category</th><th className="pb-3 font-medium">Confirmed accounts</th><th className="pb-3 font-medium">Top suggestion</th><th className="pb-3 font-medium">Status</th></tr></thead><tbody>{Object.entries(labels).map(([category,label])=>{const item=preview?.categories.find(value=>value.category===category);const suggestion=item?.suggestion.candidates[0];return <tr key={category} className="border-t"><td className="py-3 font-medium">{label}</td><td className="py-3">{item?item.confirmed.map(account=>account.name).join(', '):'—'}</td><td className="py-3 text-muted-foreground">{suggestion?`${suggestion.accountName} (${suggestion.confidence})`:'—'}</td><td className="py-3"><span className="rounded-full bg-muted px-2 py-1 text-xs">{item?'Confirmed locally':'Unavailable'}</span></td></tr>;})}</tbody></table></div>{query.isPending?<p role="status" className="text-sm text-muted-foreground">Loading local test mapping…</p>:!preview?<p role="status" className="text-sm text-muted-foreground">The local test mapping preview is unavailable. No Xero connection or mapping is assumed.</p>:<p className="text-sm text-muted-foreground">Local test preview only. Suggestions remain for review and do not change the confirmed mapping.</p>}<p className="text-sm text-muted-foreground">Mappings stay separate from Shopify trading data. An unmapped account remains unavailable; Night Scout does not assume a zero value.</p></section>;}
