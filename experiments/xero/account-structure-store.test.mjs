import test from 'node:test';import assert from 'node:assert/strict';import {writeLocalXeroAccountStructure} from './account-structure-store.mjs';
const report={selectedAccountIds:['a'],foundAccountIds:['a'],missingAccountIds:[],accountAttributeRows:1,cellCounts:[2]};
test('stores only the redacted structural diagnostic',async()=>{const writes=[];const fs={mkdir:async()=>{},writeFile:async(_p,b,o)=>writes.push({b,o}),rename:async()=>{}};await writeLocalXeroAccountStructure('/tmp/xero-structure.json',{retrievedAt:'2026-09-18T10:00:00.000Z',structure:{profitAndLoss:report,balanceSheet:report}},{fs});assert.equal(writes[0].b.includes('amount'),false);assert.equal(writes[0].o.mode,0o600);});
test('rejects raw fields and invalid shapes',async()=>{await assert.rejects(writeLocalXeroAccountStructure('/tmp/x.json',{retrievedAt:'bad',structure:{}}),/invalid/);await assert.rejects(writeLocalXeroAccountStructure('/tmp/x.json',{retrievedAt:'2026-09-18T10:00:00.000Z',structure:{profitAndLoss:{...report,raw:'no'},balanceSheet:report}}),/invalid/);});
