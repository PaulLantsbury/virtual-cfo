auto-install-peers=false
strict-peer-dependencies=false
confirm-modules-purge=false
# Temporary: Replit may restore node_modules from an incompatible workspace layout.
# Recreate dependency artifacts during non-interactive staging deployment.
force=true
