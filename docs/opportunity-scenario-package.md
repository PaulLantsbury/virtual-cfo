# Opportunity Finder and Scenario Planner — approved package

Paul approved two coding workstreams and independent review. Audit opportunity estimates, rankings, confidence and suggested plans; separate unverified source readings from examples, remove invented fallbacks and claims an unimplemented engine selected a best plan. Preserve sample arithmetic, plan gates, existing presets and handoff routes. No new ranking/financial policy, model redesign, shared engine, migration, grants, imports, Replit sync, main merge or production release.

Acceptance includes data availability/zero/failure where sources remain, sample-plan honesty, existing preset/slider/handoff interactions, access gates, desktop/mobile presentation, typecheck/build and independent review. After integration verify existing local staging and save code/tests/docs to the authorised GitHub branch. Actual opportunities and forecasts remain unfinished.

Status: complete, independently reviewed, tested and checked in local staging.

## Implementation and known limits

Opportunity Finder no longer calls the disabled/unscoped 503 endpoint or enriches recommendations with unverified marketing data. It uses existing shared synthetic examples, explicitly labels rankings/evidence/confidence and keeps one-off cash separate from recurring contribution. Monthly example sums remain unadjusted for overlap and are labelled accordingly. Existing ranking weights, three-card queue, plan gates and preset mappings remain.

Scenario Planner explicitly states no store financial data or best-plan/risk/confidence engine is connected. Fixed cards are separate from calculated sample outputs. Formulas and preset constants were independently compared and remain unchanged. Payment fee, Meta spend and Google spend sliders have no calculation effect; this is now disclosed. Inert save/compare controls are disabled. Loading presets changes the screen only; custom-preset reload retains its existing reset-to-zero behavior. Cash/runway coefficients, overlapping inputs and unreconciled financial definitions need future agreed model work, not silent fixes in this package.

Both pages remove page-local canned AI advice and misleading monitoring claims. Source availability does not certify examples. No database, financial completeness, migration/grant/import, shared engine, Replit/main or production change.

## Staging

The existing local current-source preview at http://localhost:3000, configured for staging bioalckltvkhlczusdvl, was checked with authenticated store A. Opportunity Finder shows actual analysis unavailable, GBP 31–49k unadjusted monthly examples and separate GBP 40–60k cash. Its discount preset link opens Scenario Planner, preserves selection through the store gate, applies discount −4pp/returns −1pp and removes the consumed query parameter. Scenario notices and disabled persistence controls are visible. This is local staging, not hosted deployment.

## Verification and resume

9 Opportunity and 13 Scenario browser cases pass with synthetic services, desktop/mobile gates, preset buttons, all three handoffs, reset, retained arithmetic and inactive controls. Typecheck/build and independent review pass; existing sourcemap/bundle warnings remain. Screenshots inspected. Initial Opportunity test failures were bootstrap dashboard requests, then an about:blank storage initialization error; isolated/guarded the harness without relaxing no-data-call or page-error assertions, and all nine passed.

Next: Paul’s walkthrough of these two pages. Actual recommendation engines and financial model validation remain unfinished; preserve the agreed deferred modeller/source work and unchanged Replit setup.
